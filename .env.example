# Copy this file to `.env` (which is gitignored) and fill in your key.
# `make dev` loads .env automatically; without OPENAI_API_KEY the chat uses the
# offline stub tutor.

OPENAI_API_KEY=

# Optional: override the tutor model (default: gpt-4o-mini)
# MENTOROS_MODEL=gpt-4o-mini
